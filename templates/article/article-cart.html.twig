{% block javascripts %}
    <script src="{{ asset('js/article-cart.js') }}"></script>
{% endblock %}

<div class="swiper-slide overflow-hidden">
    <div class="product-card">
        <div class="card-detail d-flex justify-content-between align-items-baseline pt-3">
            <h3 class="card-title text-uppercase">
                <a href="{{ path('app_index') }}">{{ article.articleName }}</a>
            </h3>
        </div>
        <div class="image-overlay position-relative">
            <div class="product-image">
                {% set img = article.articleImages | json_decode | first %}
                <img src="{{ asset(img) }}" alt="product-item" class="product-image img-fluid">
                <div class="text-box box-slide position-absolute">
                    <div class="text-content p-5 bg-light">
                        <h3>About the article</h3>
                        <p>Assumenda temporibus quidem ipsam. fuga corporis iusto similique voluptates sint quibusdam.</p>
                        <ul>
                            {% set elm = article.description | json_decode %}
                            {% if elm.description is defined %}
                                <li>{{ elm.description }}</li>
                            {% endif %}
                            <li>Different Size of Bed</li>
                            <li>Comfortable and Clean Room</li>
                        </ul>
                        <div class="cart-info">
                            <a href="#" class="pseudo-text-effect" data-article-id="{{ article.getArticleId() }}" data-csrf-token="{{ csrf_token('add-to-cart') }}" data-after="ADD TO CART">
                                <span>${{ article.getArticlePrice() }}</span>
                            </a>
                        </div>
                        <a href="{{ path('article_detail', {id: article.articleId}) }}" class="btn btn-normal mt-3">Learn More</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>